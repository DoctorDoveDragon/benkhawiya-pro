[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "gcc", "gnumake", "stdenv"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"