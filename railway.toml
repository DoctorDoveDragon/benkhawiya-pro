[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"

[build.buildCommand]
nixpacks = "cd backend && pip install -r requirements.txt"
